import { ActionExecutorDirective } from './action-executor.directive';
import { ActionExecutorService } from '../services/data-provider.service';

describe('DirectivesDirective', () => {
  it('should create an instance', () => {
    // const directive = new ActionExecutorDirective();
    // expect(directive).toBeTruthy();
  });
});
